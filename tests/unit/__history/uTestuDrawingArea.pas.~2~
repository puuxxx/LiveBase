unit uTestuDrawingArea;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, uDrawingArea, uBase, SysUtils, uEventModel;

type
  // Test methods for class TDrawingArea

  TestTDrawingArea = class(TTestCase)
  strict private
    FDrawingArea: TDrawingArea;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  end;

implementation

procedure TestTDrawingArea.SetUp;
begin
  FDrawingArea := TDrawingArea.Create( 11, 11 );
end;

procedure TestTDrawingArea.TearDown;
begin
  FDrawingArea.Free;
  FDrawingArea := nil;
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTDrawingArea.Suite);
end.

